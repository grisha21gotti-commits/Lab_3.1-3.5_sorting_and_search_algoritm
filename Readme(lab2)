Лабораторная работа №2

Тема: Линейные структуры данных «мультисписок / очередь / дек / приоритетная очередь» на языках Python, Java и C++.

Создание мультисписков (вложенных списков)
Python: мультисписок удобно реализовать как список списков, например `matrix = [[1, 2], [3, 4]]`. Доступ к элементам осуществляется по двум индексам `matrix[i][j]`. Для обхода таких структур используют вложенные циклы `for`.  
-Java: можно использовать `ArrayList<ArrayList<Integer>>`, где внешний список хранит ссылки на внутренние списки. Добавление новых строк и столбцов выполняется через методы `add()`.  
-C++: типичный вариант — `std::vector<std::vector<int>>`. Внешний `vector` хранит вложенные `vector`’ы, которые представляют строки или группы элементов. Элементы доступны по индексу `v[i][j]`.

Создание очереди (FIFO)  
-Python: очередь можно сделать через модуль `queue` и класс `Queue`. Элементы добавляются методом `put()`, а извлекаются из головы очереди методом `get()`.  
Java: используется интерфейс `Queue` и, например, реализация `LinkedList`. Элементы добавляют с помощью `add()/offer()`, извлекают с помощью `poll()` или `remove()`.  
-C++: в стандартной библиотеке есть контейнер `std::queue<T>`. Добавление выполняется методом `push()`, а извлечение из начала очереди — методом `pop()`. Просмотр первого элемента — `front()`.

Реализация дека (двунаправленной очереди)
Python: двунаправленную очередь удобно реализовать с помощью `collections.deque`. В неё можно добавлять и удалять элементы как слева (`appendleft()`, `popleft()`), так и справа (`append()`, `pop()`).  
-Java: используется интерфейс `Deque` и реализация `ArrayDeque`. Методы `addFirst()/addLast()` и `removeFirst()/removeLast()` позволяют работать с обоими концами очереди.  
- C++: для дека существует контейнер `std::deque<T>`. Он поддерживает доступ по индексу и операции вставки/удаления как в начале, так и в конце.

Создание приоритетной очереди
- Python: есть два популярных варианта: класс `PriorityQueue` из модуля `queue` и функции модуля `heapq`, которые реализуют приоритетную очередь на основе бинарной кучи (min-heap).  
-Java: используется класс `PriorityQueue`. По умолчанию элементы упорядочиваются в естественном порядке, но можно задать свой компаратор для другого порядка.  
- C++: в стандартной библиотеке есть контейнер `std::priority_queue<T>`, который по умолчанию является max-heap (сначала извлекается максимальный элемент).

Краткий вывод:  
Во всех трёх языках структуры данных реализованы в стандартных библиотеках, но синтаксис и детали отличаются. В Python всё опирается на динамические списки и готовые модули (`queue`, `collections`, `heapq`). В Java активно используются интерфейсы (`List`, `Queue`, `Deque`) и их реализации из `java.util`. В C++ основные структуры представлены контейнерами STL (`vector`, `queue`, `deque`, `priority_queue`), которые включаются через соответствующие заголовочные файлы.
